node {
   stage('checkout') {
    sh 'pwd';
    sh 'ls -la';
    sh 'git status';
    sh 'git log -3';
   }
   stage('build') {
   }
   stage('test') {
   }
   stage('package') {
   }
   stage('upload') {
   }
}