module.exports = {
  tags: async (tln) => [],
  options: async (tln) => [],
  dotenvs: async (tln) => ['.env'],
  inherits: async (tln) => ['nodejs', 'docker'],
  depends: async (tln) => ['node-15.11.0'],
  env: async (tln, env) => {},
  steps: async (tln) => [],
  components: async (tln) => []
}
